# Private content (book manuscript, planning docs — not for public repo)
_private/

# Environment
.env
*.pyc
__pycache__/
.venv/
venv/

# Logs
mcp_server/logs/*.log
mcp_server/logs/*.log.*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
.obsidian/

# Build / cache / deploy
.pytest_cache/
.do/

# Brain (generated embeddings — not versioned, regenerate with embed.sh)
mcp_server/brain/

# Production-only devops tools (not for public users)
mcp_server/devops/deploy.sh
mcp_server/devops/keys.sh

# Temp (one-pagers, drafts, local outputs)
temp/

# Secret scrubbing artifacts — never commit these
gitleaks-report.json
secrets.txt

# Research / internal planning docs — not for public
research/
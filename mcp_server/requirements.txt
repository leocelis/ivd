# IVD MCP Server Dependencies

# MCP protocol (pinned — newer versions break SSE transport)
mcp==1.13.1

# HTTP server (pinned — must be compatible with mcp==1.13.1)
starlette==0.49.1
sse-starlette==3.0.2
uvicorn==0.35.0

# Redis for MCP event storage (session resumability)
redis>=5.0.0

# Embeddings
numpy>=1.26.0
openai>=1.12.0
tiktoken>=0.6.0

# YAML parsing
pyyaml>=6.0

# Logging
termcolor>=2.4.0

# Testing (dev only)
pytest>=8.0.0
pytest-anyio>=0.0.0
httpx>=0.27.0
